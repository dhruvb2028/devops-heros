apiVersion: v1
kind: Service
metadata:
  name: canary-service
  namespace: session10-demo
spec:
  type: NodePort
  selector:
    app: canary-demo
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30030
