apiVersion: v1
kind: Service
metadata:
  name: recreate-service
  namespace: session10-demo
spec:
  type: NodePort
  selector:
    app: recreate-demo
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30040
